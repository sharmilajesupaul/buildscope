name: release

on:
  push:
    tags:
      - "v0.1.*"

permissions:
  contents: write
  pull-requests: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: cli/go.mod

      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm
          cache-dependency-path: ui/package-lock.json

      - name: Install UI dependencies
        run: npm --prefix ui ci

      - name: Run frontend tests
        run: npm --prefix ui test

      - name: Refresh embedded UI bundle
        run: ./scripts/refresh-embedded-ui.sh
      - name: Run Go tests
        run: go -C cli test ./...

  build:
    needs: verify
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: cli/go.mod

      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm
          cache-dependency-path: ui/package-lock.json

      - name: Install UI dependencies
        run: npm --prefix ui ci
      - name: Build release archive
        env:
          COMMIT_SHA: ${{ github.sha }}
        run: |
          rm -rf dist
          mkdir -p dist
          ./scripts/build-release.sh "${{ github.ref_name }}" "${{ matrix.goos }}" "${{ matrix.goarch }}" dist

      - name: Upload workflow artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.goos }}-${{ matrix.goarch }}
          path: dist/*

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: release-*
          merge-multiple: true
          path: dist

      - name: Generate checksums
        run: |
          cd dist
          sha256sum * > "buildscope_${GITHUB_REF_NAME#v}_checksums.txt"

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          name: BuildScope ${{ github.ref_name }}
          prerelease: ${{ startsWith(github.ref_name, 'v0.') }}
          generate_release_notes: true
          files: dist/*

  update_homebrew:
    needs: release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: release-darwin-*
          merge-multiple: true
          path: dist

      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.repository.default_branch }}

      - name: Capture macOS archive checksums
        id: checksums
        run: |
          VERSION_NO_V="${GITHUB_REF_NAME#v}"
          AMD64_SHA="$(sha256sum "dist/buildscope_${VERSION_NO_V}_darwin_amd64.tar.gz" | awk '{print $1}')"
          ARM64_SHA="$(sha256sum "dist/buildscope_${VERSION_NO_V}_darwin_arm64.tar.gz" | awk '{print $1}')"
          echo "amd64_sha=$AMD64_SHA" >> "$GITHUB_OUTPUT"
          echo "arm64_sha=$ARM64_SHA" >> "$GITHUB_OUTPUT"

      - name: Render Homebrew formula
        run: |
          ./scripts/render-homebrew-formula.sh \
            "${GITHUB_REF_NAME}" \
            "${{ steps.checksums.outputs.amd64_sha }}" \
            "${{ steps.checksums.outputs.arm64_sha }}" \
            Formula/buildscope.rb

      - name: Open formula update PR
        id: open_homebrew_pr
        continue-on-error: true
        uses: peter-evans/create-pull-request@v8
        with:
          add-paths: |
            Formula/buildscope.rb
          branch: automation/homebrew-${{ github.ref_name }}
          commit-message: "release: update Homebrew formula for ${{ github.ref_name }}"
          delete-branch: true
          title: "release: update Homebrew formula for ${{ github.ref_name }}"
          body: |
            Automated Homebrew formula update for `${{ github.ref_name }}`.

      - name: Report formula update branch
        if: steps.open_homebrew_pr.outcome == 'failure'
        run: |
          echo "::warning::GitHub Actions could not open the Homebrew PR in this repo."
          echo "::warning::Open it manually at https://github.com/${GITHUB_REPOSITORY}/pull/new/automation/homebrew-${GITHUB_REF_NAME}"
